#!/bin/bash

# script: oms-module( DEPRECATED)
# Скрипт-обертка для скрипта <oms>, обеспечивающая успешное выполнение команды
# "make set-version", а также определение установленной версии OMS из
# прикладных модулей с файлами OMS версии ниже чем 2.0.0 ( т.е. до
# переименования скрипта "oms-module" в "oms").

# Вызываем скрипт, расположенный в той же директории, что и выполняемый скрипт
# ( без учета возможности вызова с использованием линка на файл)
. $( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/oms
