Index: Config/NewModule/DB/Makefile
===================================================================
--- Config/NewModule/DB/Makefile	(revision 1672)
+++ Config/NewModule/DB/Makefile	(working copy)
@@ -592,6 +592,32 @@
 
 
 
+# build var: SKIP_CHECK_JOB
+# ‘лаг пропуска проверки корректности ( компил€ции) PL/SQL-блоков заданий
+# модул€ Scheduler ( "1" не провер€ть, по умолчанию провер€ть).
+SKIP_CHECK_JOB =
+
+# build var: SKIP_LOAD_OPTION
+# ‘лаг исключени€ загрузки параметров пакетных заданий модул€ Scheduler.  ƒл€
+# исключени€ загрузки нужно указать 1, по умолчанию загружать дл€
+# устанавливаемых пакетных заданий.
+SKIP_LOAD_OPTION =
+
+# build var: UPDATE_OPTION_VALUE
+# ‘лаг изменени€ существующих значений параметров пакетных заданий модул€
+# Scheduler при их загрузке согласно указанным в спецификации ( "1" обновл€ть,
+# по умолчанию не обновл€ть ( при этом значени€ параметров, указанные в
+# спецификации, используютс€ только при создании параметров)).
+UPDATE_OPTION_VALUE =
+
+# build var: UPDATE_SCHEDULE
+# ‘лаг обновлени€ расписани€ запуска пакетных заданий модул€ Scheduler согласно
+# указанному в спецификации ( "1" обновл€ть, по умолчанию не обновл€ть ( при
+# этом расписание в спецификации используетс€ только при создании пакетного
+# задани€)). ¬ пон€тие ¬ пон€тие расписание входит число повторных попыток и
+# интервал между повторени€ми выполнени€ батчей.
+UPDATE_SCHEDULE =
+
 # —крипты дл€ загрузки пакетных заданий и их параметров в Ѕƒ.
 installBatchTarget = \
   $(addsuffix .$(ru), \
