Index: Config/NewModule/DB/Makefile
===================================================================
--- Config/NewModule/DB/Makefile	(revision 2125)
+++ Config/NewModule/DB/Makefile	(working copy)
@@ -592,6 +592,28 @@
 
 
 
+# build var: BATCH_MASK
+# —писок масок дл€ ограничени€ устанавливаемых пакетных заданий. ѕредставл€ет
+# собой список ( через зап€тую) масок коротких наименований пакетных заданий.
+# ( можно использовать спецсимволы "*" и "?").
+#
+# ѕример: дл€ загрузки пакетных
+# заданий ClearOldLog и CheckBatchExecution маска дожна быть равна
+# "ClearOldLog,CheckBatchExecution".
+BATCH_MASK =
+
+# build var: SKIP_BATCH_MASK
+# —писок масок дл€ исключени€ устанавливаемых пакетных заданий. ѕредставл€ет
+# собой список ( через зап€тую) масок коротких наименований пакетных заданий.
+# ( можно использовать спецсимволы "*" и "?").
+#
+# ѕример: дл€ исключени€ пакетных
+# заданий, начинающихс€ с Check, маска должна быть равна "Check*".
+#
+# ћаски BATCH_MASK, SKIP_BATCH_MASK можно использовать одновременно с
+# FILE_MASK, SKIP_FILE_MASK.
+SKIP_BATCH_MASK =
+
 # build var: SKIP_CHECK_JOB
 # ‘лаг пропуска проверки корректности ( компил€ции) PL/SQL-блоков заданий
 # модул€ Scheduler ( "1" не провер€ть, по умолчанию провер€ть).
